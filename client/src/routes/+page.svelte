<script lang="ts">
	// ABOUTME: Root page — renders the followed feed as the default view.
	// ABOUTME: No data loading needed; Followed fetches its own data via stores.
	import Followed from '$lib/components/Followed.svelte'
</script>

<Followed />
