// ABOUTME: Root layout load config — runs the app as a pure client-side SPA.
// ABOUTME: All data is fetched client-side from the Go API, so SSR and prerendering are disabled.
export const ssr = false;
export const prerender = false;
